# ReAct란?

**Reason + Act**

의 줄임말입니다.

```text
Reason = 생각(추론)
Act    = 행동(Tool 호출)
```

논문 이름도

> ReAct

에서 나왔습니다.

---

# 기존 LLM 방식

일반 ChatGPT 스타일은

```text
질문
 ↓
LLM
 ↓
답변
```

예를 들어

```text
서울 현재 시간 알려줘
```

라고 하면

```text
현재 서울은 오후일 것입니다.
```

처럼 추측만 할 수 있습니다.

실제 시간을 알 수는 없습니다.

---

# ReAct 방식

ReAct는

```text
질문
 ↓
생각(Reason)           ──┐
 ↓                       │
행동(Action)             │ ->  이부분을 반복함 
 ↓                       │ 
관찰(Observation)      ──┘
 ↓
생각(Reason)
 ↓
행동(Action)
 ↓
최종답변
```

입니다.

---

예)

```text
서울 현재 시간 알려줘
```

Agent 내부

```text
Thought:
현재 시간이 필요함

Action:
get_time()

Observation:
2026-06-10 10:15:20
```

그 후

```text
Final Answer:
현재 시간은 2026-06-10 10:15:20 입니다.
```

---

# Workflow와 차이

## Workflow 방식

현재 step5.py

```text
질문
 ↓
Router
 ↓
Time
 ↓
Calculator
 ↓
Chat
 ↓
Combine
```

routing을 개발자가 미리 정함

```python
if "시간" in question:
    route="time"

if "*" in question:
    route="calculator"
```

---

## ReAct 방식

```text
질문
 ↓
LLM
 ↓
Tool 선택
 ↓
Tool 실행
 ↓
LLM
 ↓
답변
```

routing을 LLM이 결정

```python
llm.bind_tools(...)
```

---

# LangGraph에서의 ReAct

LangGraph의 가장 유명한 패턴은

```text
START
  │
  ▼
Agent
  │
  ▼
Tool
  │
  ▼
Agent
  │
  ▼
Tool
  │
  ▼
Agent
  │
  ▼
END
```

입니다.

실제로는

```text
          ┌─────────────┐
          │   Agent     │
          └──────┬──────┘
                 │
                 ▼
        ┌─────────────────┐
        │ tools_condition │
        └────────┬────────┘
                 │
        ┌────────┴────────┐
        ▼                 ▼
     ToolNode            END
        │
        └──────► Agent
```

형태가 됩니다.

---

한 줄로 요약하면,

> **ReAct 방식은 "LLM이 생각(Reason)하고, 필요하면 Tool을 실행(Act)하고, 결과를 관찰(Observation)한 뒤 다시 생각하는 Agent 구조"입니다.**

---

# 아래는 step5.py의 문제점 확인

---

# 1. step5.py에서는 Tool 선택을 LLM이 하지 못함

현재 구조 router() 함수에서 다음 node를 선택하기 위해 아래와 같이 직접 비교합니다. 이를 해결 하기 위해 router 기능을 LLM을 이용하여 처리하는 것를 ReAct 구조라고 합니다.

```python
if "시간" in question:
    routes.append("time")

if "*" in question:
    routes.append("calculator")

if "Docker" in question:
    routes.append("chat")
```

즉,

```text
개발자
  ↓
직접 판단
  ↓
Tool 선택
```

입니다.

---

추가 질문을 아래과 같이 하면

```text
서울 현재 시각 알려줘
```

현재 Router는

```python
if "시간" in question:
```

만 검사

결과

```text
time 호출 안됨
```

---

ReAct 구조

```text
질문
 ↓
LLM
 ↓
"시각"도 시간 관련이네
 ↓
get_time()
```

자동 판단

---

# 2. 기존 router() 방식은 새로운 Tool 추가 시 Router 수정 필요

현재

```text
Time
Calculator
Chat
```

3개만 있음

만약

```text
Weather
SQL
RAG
PDF
Email
Slack
```

추가되면

Router가 계속 커짐

```python
if ...
if ...
if ...
if ...
if ...
```

---

실무에서는

```text
Tool 30~100개
```

도 흔함

---

ReAct 구조는 아래의 tools 배열에 tools 함수만 추가하면 기능 확장을 할 수 있습니다

```python
tools = [
    time_tool,
    weather_tool,
    sql_tool,
    rag_tool
]
```

등록만 하면 됨

---

# 3. router() 방식은 자연어 이해 능력이 없음

현재

```python
if "Docker" in question:
```

---

가능

```text
Docker가 뭐야?
```

---

불가능

```text
컨테이너 플랫폼 설명해줘

Docker 기술 알려줘

도커란?
```

---

ReAct 방식은 LLM이 의미를 이해

```text
Docker
도커
컨테이너 플랫폼
컨테이너 엔진
```

모두 처리 기능합니다

---

# 4. step5.py 방식은 순차 실행(파이프 라인 방식)이 안됨

현재

```text
Router
 ├─ Time
 ├─ Calculator
 └─ Chat
```

---

질문

```text
현재 시간을 확인하고

그 시간을 DB에 저장하고

저장 결과를 알려줘
```

---

필요한 흐름

```text
Time
 ↓
DB
 ↓
Answer
```

---

현재 step5.py는 병렬 분기 만 가능합니다.

---

ReAct

```text
Thought
 ↓
Time Tool
 ↓
Observation
 ↓
DB Tool
 ↓
Observation
 ↓
Answer
```

가능

---

# 5. step5.py는 Tool 결과를 기반으로 다음 Tool 선택 불가

현재

```text
Router
```

는 질문만 보고 결정

---

예

```text
현재 시간이 오전이면
A 실행

오후면
B 실행
```

---

현재 구조

불가능

왜냐하면

```text
Router 시점에는
Time 결과가 없음
```

---

ReAct

```text
Time Tool
 ↓
오전 확인
 ↓
A Tool 선택
```

가능

---

# 6. 무한 확장이 어려움

현재

```text
질문
 ↓
Router
 ↓
Tool
 ↓
END
```

---

ReAct

```text
질문
 ↓
Agent
 ↓
Tool
 ↓
Agent
 ↓
Tool
 ↓
Agent
 ↓
END
```

필요할 때까지 반복

---

예

```text
AWS EC2 가격 조사하고

서울 날씨 조사하고

그 결과를 표로 정리해줘
```

---

Agent

```text
Search Tool
 ↓
Search Tool
 ↓
Calculator Tool
 ↓
Answer
```

자동 반복

---

# 7. Memory(대화 기억) 활용 부족

현재

```python
question
```

만 있음

---

이전 대화

```text
사용자:
서울 날씨 알려줘

사용자:
그럼 부산은?
```

---

현재 구조

```text
그럼 부산은?
```

의 "그럼"을 이해 못함

---

ReAct Agent

```text
messages
```

전체 대화 사용

가능

---

# 8. 실무에서 가장 큰 차이

현재 step5

```text
Workflow
```

입니다.

즉

```text
정해진 길
```

을 따라감

---

ReAct

```text
Agent
```

입니다.

즉

```text
상황에 따라
스스로 판단
```

합니다.

---

비유하면

### step5.py

```text
자동 세차기
```

입력

```text
차 넣기
```

출력

```text
세차 완료
```

정해진 순서

---

### ReAct Agent

```text
운전기사
```

상황 보고

```text
주유 필요
 ↓
주유소 방문

타이어 문제
 ↓
정비소 방문

목적지 변경
 ↓
경로 재탐색
```

판단 가능

---

# step5.py를 ReAct Agent 스타일로 변경

## 1. Tool 정의

```python
from langchain_core.tools import tool
from datetime import datetime


@tool
def get_time() -> str:
    """현재 시간을 반환"""
    return str(datetime.now())


@tool
def calculator(expression: str) -> str:
    """수식을 계산"""

    try:
        return str(eval(expression))
    except Exception as e:
        return str(e)


@tool
def explain_docker() -> str:
    """Docker 설명"""

    return """
Docker는 컨테이너 기반 애플리케이션
실행 플랫폼입니다.
"""
```

---

## 2. LLM 생성

OpenAI 예제

```python
from langchain_openai import ChatOpenAI

llm = ChatOpenAI(
    model="gpt-4.1-mini",   
    api_key=os.getenv("OPENAI_API_KEY"), 
    temperature=0
    )
```

---

## 3. Tool 바인딩

```python
#tools 관련 함수 배열 선언 
tools = [
    get_time,
    calculator,
    explain_docker
]

# LLM에게 `이런 Tool들이 존재한다` 라고 알려주는 것입니다
llm_with_tools = llm.bind_tools(tools)
```

---

## 4. ReAct Agent Node

LLM이 요청한 Tool을 실제로 실행 할 수 있는 객체를 생성합니다

```python
from langgraph.prebuilt import ToolNode
from langgraph.prebuilt import tools_condition


#LLM이 요청한 Tool을 실제로 실행 할 수 있는 객체를 생성합니다
tool_node = ToolNode(tools)
```

---

## 5. State

LangGraph에서는 모든 Node가 공유하는 데이터를 State 라고 합니다.

Agent가 생각하려면

```text
사용자 질문

이전 답변

Tool 결과
```

를 모두 기억해야 합니다.

ReAct는

```text
HumanMessage
 ↓
AIMessage
 ↓
ToolMessage
 ↓
AIMessage
```

가 계속 누적되어야 합니다.

---

초기

```python
{
    "messages": [
        HumanMessage("현재 시간 알려줘")
    ]
}
```

---

Agent 실행

```python
return {
    "messages": [
        AIMessage(tool_calls=[...])
    ]
}
```

---

여기서 LangGraph가 일반 필드처럼 처리하면

기존

```python
[
    HumanMessage(...)
]
```

가 사라지고

---

결과

```python
[
    AIMessage(...)
]
```

만 남음

---

엄청 큰 문제(이전 질문이 사라짐) 발생 됩니다.

---

왜냐하면

Agent가 다시 실행될 때

```text
사용자 질문
```

을 잃어버렸기 때문

---

# 3. 실제로 원하는 것은?

우리가 원하는 건 덮어쓰기

```python
[
    AIMessage(...)
]
```

가 아니라

---

추가

```python
[
    HumanMessage(...),

    AIMessage(...)
]
```

입니다.

---

즉

```text
append
```

가 필요합니다.

---

# 4. add_messages 의 역할

바로 이것을 위해 존재합니다.

```python
from langgraph.graph.message import add_messages
```

---

이 함수는

```text
messages 필드는

덮어쓰지 말고

추가(append)해라
```

라고 LangGraph에게 알려주는 것입니다.

---

# 5. Annotated 란?

파이썬 타입 힌트 기능

```python
from typing import Annotated
```

---

일반 타입

```python
age: int
```

---

추가 정보 포함

```python
age: Annotated[int, "추가설명"]
```

---

LangGraph는 이것을 이용해서

```python
messages: Annotated[
    list[BaseMessage],
    add_messages
]
```

를 해석합니다.

---

즉

```text
messages 타입은

list[BaseMessage]

그리고

업데이트 방식은

add_messages 사용
```

이라는 의미

---

# 6. LangGraph 내부에서 무슨 일이 일어날까?

초기 상태

```python
{
    "messages": [
        HumanMessage("현재 시간 알려줘")
    ]
}
```

---

Agent 반환

```python
{
    "messages": [
        AIMessage(
            tool_calls=[...]
        )
    ]
}
```

---

# 7. add_messages 있음

LangGraph 내부적으로

```python
state["messages"].extend(
    new_messages
)
```

비슷하게 처리

---

결과

```python
[
    HumanMessage(...),

    AIMessage(...)
]
```

---

메시지 유지

---

# 8. ToolNode 실행 후

ToolNode 반환

```python
{
    "messages": [
        ToolMessage(
            content="2026-06-10 20:00"
        )
    ]
}
```

---

add_messages 적용

결과

```python
[
    HumanMessage(...),

    AIMessage(
        tool_calls=[...]
    ),

    ToolMessage(...)
]
```

---

계속 누적

---

# 9. 다시 Agent 실행

Agent는

```python
state["messages"]
```

전체를 받음

---

즉

```python
[
    HumanMessage(...),

    AIMessage(...),

    ToolMessage(...)
]
```

모두 확인 가능

---

그래서

```text
아 Tool 결과가 왔네

이제 최종 답변하자
```

판단 가능

---

```python
from typing import TypedDict
from langchain_core.messages import BaseMessage

# ReAct Agent에서는 메시지가 누적되어야 합니다.
class AgentState(TypedDict):
    # BaseMessage은 LangChain에서는 모든 메시지의 부모 클래스
    messages: Annotated[
        list[BaseMessage],
        add_messages
    ]
```

---

## 6. Agent Node

step5.py의 router_node()함수와 유사합니다. router_node()함수에서는 if 문을 사용하였지만, LLM에서는 AI가 추론을 해서 다음에 실행할 tool 정보를 리턴합니다. tool을 실행한 결과에 대하여 Agent는 아래의 함수를 계속 반복 실행할 수 있습니다.

```python
def agent_node(state: AgentState):

    response = llm_with_tools.invoke(
        state["messages"]
    )

    return {
        "messages": [response]
    }
```

---

## 7. Graph

```python
from langgraph.graph import StateGraph
from langgraph.graph import START, END

builder = StateGraph(AgentState)

builder.add_node(
    "agent",
    agent_node
)

builder.add_node(
    "tools",
    tool_node
)

builder.add_edge(
    START,
    "agent"
)

builder.add_conditional_edges(
    "agent",
    tools_condition,
    {
        "tools": "tools",
        "__end__": END
    }
)

builder.add_edge(
    "tools",
    "agent"
)

graph = builder.compile()
```

---

## 8. 실행

```python
from langchain_core.messages import HumanMessage

result = graph.invoke(
    {
        "messages": [
            HumanMessage(
                content="""
현재 시간을 알려주고,
100 * 200을 계산한 다음,
Docker가 무엇인지 설명해줘.
"""
            )
        ]
    }
)

print("\n===================")
print("최종 결과")
print("===================")

print(
    result["messages"][-1].content
)

```

---

# 전체 코드

파일명 : step6.py

```python
import os
from typing import TypedDict
from typing import Annotated
from datetime import datetime

from langchain_core.tools import tool
from langchain_core.messages import HumanMessage
from langgraph.graph.message import add_messages
from langgraph.graph import StateGraph
from langgraph.graph import START, END
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import ToolNode
from langgraph.prebuilt import tools_condition

from typing import TypedDict
from langchain_core.messages import BaseMessage

class AgentState(TypedDict):
    # BaseMessage은 LangChain에서는 모든 메시지의 부모 클래스
    messages: Annotated[
        list[BaseMessage],
        add_messages
    ]

llm = ChatOpenAI(
    model="gpt-4.1-mini",   
    api_key=os.getenv("OPENAI_API_KEY"), 
    temperature=0
    )

@tool
def get_time() -> str:
    """현재 시간을 반환"""
    return str(datetime.now())


@tool
def calculator(expression: str) -> str:
    """수식을 계산"""

    try:
        return str(eval(expression))
    except Exception as e:
        return str(e)


@tool
def explain_docker() -> str:
    """Docker 설명"""

    return """
Docker는 컨테이너 기반 애플리케이션
실행 플랫폼입니다.
"""


#tools 관련 함수 배열 선언 
tools = [
    get_time,
    calculator,
    explain_docker
]

# LLM에게 `이런 Tool들이 존재한다` 라고 알려주는 것입니다
llm_with_tools = llm.bind_tools(tools)


#LLM이 요청한 Tool을 실제로 실행 할 수 있는 객체를 생성합니다
tool_node = ToolNode(tools)


def agent_node(state: AgentState):

    response = llm_with_tools.invoke(
        state["messages"]
    )

    return {
        "messages": [response]
    }


# =====================================================
# Graph 생성
# =====================================================

builder = StateGraph(AgentState)

# Node 등록
builder.add_node(
    "agent",
    agent_node
)

builder.add_node(
    "tools",
    tool_node
)

builder.add_edge(
    START,
    "agent"
)

builder.add_conditional_edges(
    "agent",
    tools_condition,
    {
        "tools": "tools",
        "__end__": END
    }
)

builder.add_edge(
    "tools",
    "agent"
)

# Graph 컴파일
graph = builder.compile()


# =====================================================
# 실행
# =====================================================
result = graph.invoke(
    {
        "messages": [
            HumanMessage(
                content="""
현재 시간을 알려주고,
100 * 200을 계산한 다음,
Docker가 무엇인지 설명해줘.
"""
            )
        ]
    }
)

print("\n===================")
print("최종 결과")
print("===================")

print(
    result["messages"][-1].content
)
```

# 실행 흐름

질문:

```text
현재 시간을 알려주고,
100 * 200을 계산한 다음,
Docker가 무엇인지 설명해줘.
```

Agent 내부에서는

```text
Thought:
시간이 필요함

Action:
get_time()

Observation:
2026-06-10 10:00
```

↓

```text
Thought:
계산 필요

Action:
calculator("100*200")

Observation:
20000
```

↓

```text
Thought:
Docker 설명 필요

Action:
explain_docker()

Observation:
Docker는...
```

↓

```text
Final Answer:
현재 시간은 ...
100*200 결과는 ...
Docker는 ...
```

---

# step5 방식과 ReAct 방식 비교

| 항목         | step5.py    | ReAct Agent  |
| ------------ | ----------- | ------------ |
| Tool 선택    | Router 코드 | LLM          |
| Tool 추가    | Router 수정 | Tool 등록만  |
| 유지보수     | 어려움      | 쉬움         |
| Tool 수 3개  | 적합        | 적합         |
| Tool 수 50개 | 불편        | 적합         |
| 실무 활용    | Workflow    | Agent        |

실무에서는 보통

```text
LangGraph
   │
   ├─ Workflow Node
   │
   └─ ReAct Agent Node
          │
          ├─ Time Tool
          ├─ Search Tool
          ├─ SQL Tool
          ├─ RAG Tool
          └─ Calculator Tool
```

처럼 **LangGraph 안에 ReAct Agent를 하나의 Node로 넣는 하이브리드 구조**를 가장 많이 사용합니다.

---

# 순수 ReAct Agent

방금 작성한 코드는 사실상

```text
START
  │
  ▼
Agent
  │
  ▼
ToolNode
  │
  ▼
Agent
  │
  ▼
END
```

인 **순수 ReAct Agent** 구조입니다.

실무에서 말하는 **하이브리드 구조**로 바꾸려면

```text
START
  │
  ▼
Preprocess
  │
  ▼
Agent
  │
  ▼
ToolNode
  │
  ▼
Agent
  │
  ▼
Postprocess
  │
  ▼
END
```

처럼 Workflow Node가 Agent를 감싸야 합니다.

---

# 하이브리드 구조 예제

## State

```python
from typing import TypedDict, Annotated
from langgraph.graph.message import add_messages
from langchain_core.messages import BaseMessage

class AgentState(TypedDict):

    messages: Annotated[
        list[BaseMessage],
        add_messages
    ]

    user_question: str

    final_answer: str
```

---

# Workflow Node 1

질문 전처리

```python
def preprocess_node(state: AgentState):

    print("[Preprocess]")

    question = state["messages"][-1].content

    return {
        "user_question": question
    }
```

---

# ReAct Agent

기존 코드 그대로

```python
def agent_node(state: AgentState):

    response = llm_with_tools.invoke(
        state["messages"]
    )

    return {
        "messages": [response]
    }
```

---

# Workflow Node 2

최종 결과 정리

```python
def postprocess_node(state: AgentState):

    print("[Postprocess]")

    final_msg = state["messages"][-1]

    return {
        "final_answer": final_msg.content
    }
```

---

# Graph 구성

```python
builder = StateGraph(AgentState)
```

---

Workflow Node 등록

```python
builder.add_node(
    "preprocess",
    preprocess_node
)

builder.add_node(
    "postprocess",
    postprocess_node
)
```

---

ReAct Node 등록

```python
builder.add_node(
    "agent",
    agent_node
)

builder.add_node(
    "tools",
    ToolNode(tools)
)
```

---

흐름

```python
builder.add_edge(
    START,
    "preprocess"
)

builder.add_edge(
    "preprocess",
    "agent"
)
```

---

Agent → Tool 또는 Postprocess

```python
def route_after_agent(state):

    last = state["messages"][-1]

    if hasattr(last, "tool_calls") and last.tool_calls:
        return "tools"

    return "postprocess"
```

---

조건 분기

```python
builder.add_conditional_edges(
    "agent",
    route_after_agent,
    {
        "tools": "tools",
        "postprocess": "postprocess"
    }
)
```

---

Tool 실행 후

```python
builder.add_edge(
    "tools",
    "agent"
)
```

---

마무리

```python
builder.add_edge(
    "postprocess",
    END
)
```

---

# 실행 흐름

질문

```text
현재 시간을 알려주고
100 * 200 계산하고
Docker 설명해줘
```

실행

```text
START
 │
 ▼
Preprocess
 │
 ▼
Agent
 │
 ▼
ToolNode
 │
 ▼
Agent
 │
 ▼
ToolNode
 │
 ▼
Agent
 │
 ▼
Postprocess
 │
 ▼
END
```

---

# 왜 이것이 하이브리드인가?

순수 ReAct

```text
START
 │
 ▼
Agent
 │
 ▼
Tool
 │
 ▼
Agent
 │
 ▼
END
```

---

하이브리드

```text
START
 │
 ▼
Workflow
 │
 ▼
Agent
 │
 ▼
Tool
 │
 ▼
Agent
 │
 ▼
Workflow
 │
 ▼
END
```

---

실무에서는 더 발전해서

```text
START
 │
 ▼
사용자 인증
 │
 ▼
권한 확인
 │
 ▼
RAG Agent
 │
 ▼
결과 검증
 │
 ▼
DB 저장
 │
 ▼
END
```

처럼 사용합니다.

여기서

```text
사용자 인증
권한 확인
결과 검증
DB 저장
```

은 Workflow Node이고,

```text
RAG Agent
```

만 ReAct Agent입니다.

그래서 실무에서는 보통

```python
builder.add_node(
    "rag_agent",
    rag_agent_graph
)
```

처럼 **ReAct Agent 전체를 하나의 Node처럼 취급**하여 큰 LangGraph 안에 넣습니다. 이것이 "LangGraph 안에 ReAct Agent를 하나의 Node로 넣는 하이브리드 구조"의 가장 전형적인 형태입니다.

# 전체 코드

파일명 : step7.py

```python
"""
Hybrid LangGraph Example

Workflow + ReAct Agent

START
  │
  ▼
Preprocess
  │
  ▼
Agent
  │
  ▼
tools_condition
  │
 ┌───────────────┐
 ▼               ▼
ToolNode     Postprocess
 │               │
 └────► Agent ◄──┘
                 │
                 ▼
                END
"""

import os
from datetime import datetime

from typing import TypedDict
from typing import Annotated

from langchain_openai import ChatOpenAI

from langchain_core.tools import tool
from langchain_core.messages import (
    BaseMessage,
    HumanMessage
)

from langgraph.graph import (
    StateGraph,
    START,
    END
)

from langgraph.graph.message import add_messages

from langgraph.prebuilt import (
    ToolNode
)


# =====================================================
# State
# =====================================================
#
# Workflow Node와 Agent가
# 함께 사용하는 공유 메모리
#
# =====================================================

class AgentState(TypedDict):

    # ReAct Agent의 핵심 메모리
    messages: Annotated[
        list[BaseMessage],
        add_messages
    ]

    # Workflow에서 사용할 데이터
    user_question: str

    # 최종 결과
    final_answer: str


# =====================================================
# LLM
# =====================================================

llm = ChatOpenAI(
    model="gpt-4.1-mini",
    api_key=os.getenv("OPENAI_API_KEY"),
    temperature=0
)


# =====================================================
# Tool 정의
# =====================================================

@tool
def get_time() -> str:
    """
    현재 시간을 반환
    """

    return str(datetime.now())


@tool
def calculator(expression: str) -> str:
    """
    수식을 계산
    """

    try:
        return str(eval(expression))

    except Exception as e:
        return str(e)


@tool
def explain_docker() -> str:
    """
    Docker 설명
    """

    return """
Docker는 컨테이너 기반 애플리케이션
실행 플랫폼입니다.
"""


# =====================================================
# Tool 등록
# =====================================================

tools = [
    get_time,
    calculator,
    explain_docker
]


# =====================================================
# LLM에게 Tool 정보를 제공
# =====================================================

llm_with_tools = llm.bind_tools(
    tools
)


# =====================================================
# Tool 실행 Node
# =====================================================

tool_node = ToolNode(
    tools
)


# =====================================================
# Workflow Node #1
# =====================================================
#
# 사용자 질문 전처리
#
# =====================================================

def preprocess_node(state: AgentState):

    print("\n[Preprocess Node 실행]")

    question = state["messages"][-1].content

    print(f"질문: {question}")

    return {
        "user_question": question
    }


# =====================================================
# ReAct Agent Node
# =====================================================
#
# LLM이
#
# 1. Tool 사용 여부 판단
# 2. Tool Call 생성
# 3. 최종 답변 생성
#
# =====================================================

def agent_node(state: AgentState):

    print("\n[Agent Node 실행]")

    response = llm_with_tools.invoke(
        state["messages"]
    )

    return {
        "messages": [response]
    }


# =====================================================
# Workflow Node #2
# =====================================================
#
# 최종 결과 정리
#
# =====================================================

def postprocess_node(state: AgentState):

    print("\n[Postprocess Node 실행]")

    final_message = state["messages"][-1]

    return {
        "final_answer": final_message.content
    }


# =====================================================
# Conditional Router
# =====================================================
#
# Agent 결과 분석
#
# Tool 호출이 있으면
#   -> ToolNode
#
# 없으면
#   -> Postprocess
#
# =====================================================

def route_after_agent(state: AgentState):

    last_message = state["messages"][-1]

    # Tool 호출 존재
    if hasattr(last_message, "tool_calls"):

        if last_message.tool_calls:

            print(
                "\n[Router] Tool 호출 감지"
            )

            return "tools"

    print(
        "\n[Router] 최종 답변 생성 완료"
    )

    return "postprocess"


# =====================================================
# Graph 생성
# =====================================================

builder = StateGraph(
    AgentState
)


# =====================================================
# Node 등록
# =====================================================

builder.add_node(
    "preprocess",
    preprocess_node
)

builder.add_node(
    "agent",
    agent_node
)

builder.add_node(
    "tools",
    tool_node
)

builder.add_node(
    "postprocess",
    postprocess_node
)


# =====================================================
# Edge 연결
# =====================================================

builder.add_edge(
    START,
    "preprocess"
)

builder.add_edge(
    "preprocess",
    "agent"
)


# =====================================================
# Agent -> Tool 또는 Postprocess
# =====================================================

builder.add_conditional_edges(
    "agent",
    route_after_agent,
    {
        "tools": "tools",
        "postprocess": "postprocess"
    }
)


# =====================================================
# Tool 실행 후 Agent 재호출
# =====================================================

builder.add_edge(
    "tools",
    "agent"
)


# =====================================================
# 종료
# =====================================================

builder.add_edge(
    "postprocess",
    END
)


# =====================================================
# Compile
# =====================================================

graph = builder.compile()


# =====================================================
# 실행
# =====================================================

result = graph.invoke(
    {
        "messages": [
            HumanMessage(
                content="""
현재 시간을 알려주고,
100 * 200을 계산한 다음,
Docker가 무엇인지 설명하고,
AI에 대하여 1000자로 설명해줘.
"""
            )
        ]
    }
)


# =====================================================
# 결과 출력
# =====================================================

print("\n")
print("=" * 60)
print("최종 결과")
print("=" * 60)

print(
    result["final_answer"]
)
```
