# **Retriever(검색 작업)**

**Retriever(검색기, 검색 모듈)** 는 사용자의 질문(Query)에 대해 **관련성이 높은 문서(Document)를 찾아서 반환하는 컴포넌트**입니다. 특히 RAG(Retrieval-Augmented Generation) 시스템에서 매우 중요한 역할을 담당합니다. 

쉽게 말하면:

> LLM = 답변하는 사람
> Retriever = 자료를 찾아주는 사서(Librarian)

라고 생각하면 됩니다.

---

# 1. Retriever가 필요한 이유

LLM은 학습된 데이터만 알고 있습니다.

예를 들어:

```text
질문:
"2026년 하나은행 투자설명서 내용을 요약해줘"
```

GPT는 해당 PDF를 학습하지 않았기 때문에 모릅니다.

이때 Retriever가:

```text
PDF 문서
↓
관련 내용 검색
↓
LLM에게 전달
↓
답변 생성
```

과정을 수행합니다.

---

# 2. RAG 구조에서 Retriever 위치

```text
사용자 질문
      ↓
  Retriever
      ↓
관련 문서 검색
      ↓
    LLM
      ↓
   답변 생성
```

조금 더 자세히 보면

```text
질문
 ↓
Embedding 생성
 ↓
Vector DB 검색
 ↓
Top-K 문서 추출
 ↓
Prompt 생성
 ↓
LLM
 ↓
응답
```

입니다.

---

# 3. Retriever 동작 예제

문서가 다음과 같이 저장되어 있다고 가정합니다.

```text
문서1:
문서1 page=8
결론

문서2 page=2
연구 목적

문서3 page=1
노지 스마트농업 시범사업 설명
```

사용자 질문

```text
노지 스마트농업에 대해 알려줘
```

Retriever 검색 결과

```text
문서1 page=8
IV. 결 론
기상환경 조절이 어려운 노지재배에서 가뭄 혹은 습해에 
따른 급격한 생산량 저하에 대응할 수 있는 의사지원시스템
...

문서2 page=2
련 시스템을 찾아보기 어려운 실정이다.
본 연구에서는 밀을 대상으로 재배관리 의사결정 지원시스
템을 웹 기반으로 설계하고 구축하고자 한다. 
...

문서3 page=1
과정기반 작물모형을 이용한 웹 기반 밀 재배관리 의사결정 지원시스템 설계 및 구축
18 • Journal of the Korean Society of Agricultural Engineers, 66(4), 2024. 7
터 기반의 노지 스마트농업 시범사업을 2020년부터 운영하고 
...
```

LLM 입력

```text
질문:
당신은 스마트농업 전문가입니다.

반드시 제공된 문서 내용만 사용하여 답변하세요.

문서:
{context}

질문:
{query}

답변 작성 규칙:

1. 문서에 있는 내용만 사용한다.
2. 문서에 없는 내용은 추측하지 않는다.
3. 핵심 내용을 요약한다.
4. 답변 마지막에 참고 페이지를 표시한다.
```

---

# 4. LangChain에서 Retriever

LangChain에서는 Retriever를 매우 쉽게 생성할 수 있습니다.

예:

```python
retriever = vectorstore.as_retriever()
```

질문 수행

```python
docs = retriever.invoke(
    "노지 스마트농업에 대해 알려줘"
)
```

결과

```python
[
 Document(
   page_content="노지 스마트농업에 대해 알려줘"
 )
]
```

LangChain에서도 Retriever는 "질문을 받아 문서를 반환하는 인터페이스"로 정의됩니다. ([LangChain Docs][1])

---

# 5. Retriever와 Vector DB의 차이

많은 초보자가 헷갈립니다.

| 구성요소        | 역할               |
| --------------- | ------------------ |
| Embedding Model | 텍스트 → 벡터 변환 |
| Vector DB       | 벡터 저장          |
| Retriever       | 검색 수행          |
| LLM             | 답변 생성          |

예:

```text
ChromaDB
├── 벡터 저장
├── 벡터 검색
└── Retriever 제공
```

즉

```text
Vector DB = 창고
Retriever = 창고 관리자
```

라고 생각하면 됩니다.

---

# 6. 핵심 정리

```text
Retriever란?

사용자의 질문과 가장 관련성이 높은
문서(Chunk)를 찾아서 반환하는 검색 모듈
```

RAG의 핵심 3단계는

```text
1. Retrieve (검색)
      ↓
2. Augment (문맥 추가)
      ↓
3. Generate (생성)
```

이며,

```text
Retriever 품질
=
RAG 품질
```

이라고 해도 과언이 아닙니다. 실제로 RAG 성능 문제의 상당수는 LLM이 아니라 **잘못된 Chunk를 검색하는 Retriever 문제**에서 발생합니다.

아래의 예제는 검색에 대한 결과를 찾아 점수와 내용을 출력하는 예제입니다.

---

# 7. 전체 예제 코드

>파일명 : step6.py

```python
from langchain_chroma import Chroma
from langchain_huggingface import HuggingFaceEmbeddings

##############################################################################
# 설정 정보
##############################################################################

# ChromaDB Collection 이름
COLLECTION_NAME = "pdf_collection"

# ChromaDB가 저장된 디렉토리
PERSIST_DIRECTORY = "./chroma_db"

# 문장을 벡터로 변환할 임베딩 모델
EMBEDDING_MODEL = (
    "sentence-transformers/all-MiniLM-L6-v2"
)

##############################################################################
# 임베딩 모델 생성
##############################################################################

def get_embedding_model():
    """
    HuggingFace 임베딩 모델 생성

    역할:
        텍스트를 숫자 벡터(Vector)로 변환

    예)
        "스마트 농업"
            ↓
        [0.123, 0.456, 0.789, ...]

    Retriever는 질문과 문서를 같은 벡터 공간으로 변환한 후
    유사도를 계산하여 검색을 수행한다.
    """

    return HuggingFaceEmbeddings(
        model_name=EMBEDDING_MODEL
    )

##############################################################################
# Chroma Vector DB 로드
##############################################################################

def load_vector_store():
    """
    이미 생성된 ChromaDB를 로드

    ChromaDB 내부 구조

    ChromaDB
    ├── 문서 Chunk
    ├── Embedding Vector
    └── Metadata

    Retriever는 이 Vector DB를 이용하여 검색한다.
    """

    embedding_model = get_embedding_model()

    vector_store = Chroma(
        collection_name=COLLECTION_NAME,
        persist_directory=PERSIST_DIRECTORY,
        embedding_function=embedding_model
    )

    return vector_store

##############################################################################
# Retriever 생성
##############################################################################

def create_retriever():
    """
    Retriever 객체 생성

    search_type="similarity"

        질문과 가장 유사한 문서를 검색

    k=3

        상위 3개 문서를 반환

    반환 형태

        List[Document]
    """

    vector_store = load_vector_store()

    retriever = vector_store.as_retriever(
        search_type="similarity",
        search_kwargs={
            "k": 3
        }
    )

    return retriever

##############################################################################
# Retriever 검색
##############################################################################

def search(query):
    """
    Retriever를 이용한 검색

    입력:
        사용자 질문

    예)
        "노지 스마트농업 시범사업이 무엇인가?"

    처리 과정

        질문
            ↓
        Embedding 생성
            ↓
        ChromaDB 검색
            ↓
        상위 3개 문서 반환

    반환 형태

        [
            Document(...),
            Document(...),
            Document(...)
        ]
    """

    retriever = create_retriever()

    return retriever.invoke(query)

##############################################################################
# 점수(score)까지 확인하는 검색
##############################################################################

def search_with_score(query, k=3):
    """
    Retriever 대신 ChromaDB를 직접 사용

    similarity_search_with_score()

    반환 형태

        [
            (Document, score),
            (Document, score),
            (Document, score)
        ]

    score 의미

        Chroma는 Distance 값을 반환

        값이 작을수록 유사도가 높음

        예)

        0.12 ← 매우 유사
        0.35 ← 유사
        0.78 ← 관련성 낮음
        1.20 ← 거의 무관
    """

    vector_store = load_vector_store()

    return vector_store.similarity_search_with_score(
        query,
        k=k
    )

##############################################################################
# 실행 테스트
##############################################################################

# 사용자 질문
query = "농작물 재배관리 의사결정에 대해 알려줘"

# 검색 수행
docs = search_with_score(query)

print(f"질문: {query}")

##############################################################################
# 검색 결과 출력
##############################################################################

for idx, (doc, score) in enumerate(docs):

    print("=" * 80)

    print(f"검색 결과 #{idx+1}")

    print(f"유사도 거리(score): {score:.4f}")

    print(f"페이지 번호: {doc.metadata.get('page')}")

    print(f"파일명: {doc.metadata.get('source')}")

    print("\n[문서 내용 일부]\n")

    print(doc.page_content[:300])

    print("\n")
```

---

# 8. 실행 결과

유사도 거리(score)와 페이지와 내용을 확인 합니다.

```text
Loading weights: 100%|█████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 103/103 [00:00<00:00, 10062.50it/s]
질문: 농작물 재배관리 의사결정에 대해 알려줘
================================================================================
검색 결과 #1
유사도 거리(score): 0.7883
페이지 번호: 2
파일명: data/과정기반 작물모형을 이용한 웹 기반 밀 재배관리 의사결정 지원시스템 설계 및 구축.pdf

[문서 내용 일부]

김솔희⋅석승원⋅청리광⋅장태일⋅김태곤
한국농공학회논문집 제66권 제4호, 2024 • 19
관련 시스템을 찾아보기 어려운 실정이다.
본 연구에서는 밀을 대상으로 재배관리 의사결정 지원시스
템을 웹 기반으로 설계하고 구축하고자 한다. 과정기반 모형
을 이용하여 기상여건을 평가하고, 향후 기상을 고려하여 시
나리오 분석을 통해 생산량을 예측한다. 재배기간 동안 매일 
평가가 이루어지고, 분석 결과가 웹기반 대시보드에 업데이
트되어 의사결정을 돕는다. 과정기반 작물모형은 APSIM을 
이용하였으며, 재배실험을 통해 수집한 자료를 바탕으로 


================================================================================
검색 결과 #2
유사도 거리(score): 0.8410
페이지 번호: 0
파일명: data/과정기반 작물모형을 이용한 웹 기반 밀 재배관리 의사결정 지원시스템 설계 및 구축.pdf

[문서 내용 일부]

가 노지이며, 농가 단위 경지면적이 2 ha 이하인 소규모 농가
인 실정을 고려하여 노지 스마트 디지털농업을 접근할 필요
성을 제기하였다. 농림축산식품부는 노동집약적이고 경험 위
주의 관행농법 위주의 노지재배 방식을 탈피하여 노동력을 
절감하는 동시에 안정적인 생산과 생산성 향상을 위해 데이
a Assistant Research Professor, Department of Smart Farm, Jeonbuk National 
University
b Undergraduate, Department of Smart Farm, Jeonbu


================================================================================
검색 결과 #3
유사도 거리(score): 0.8596
페이지 번호: 8
파일명: data/과정기반 작물모형을 이용한 웹 기반 밀 재배관리 의사결정 지원시스템 설계 및 구축.pdf

[문서 내용 일부]

김솔희⋅석승원⋅청리광⋅장태일⋅김태곤
한국농공학회논문집 제66권 제4호, 2024 • 25
IV. 결 론
기상환경 조절이 어려운 노지재배에서 가뭄 혹은 습해에 
따른 급격한 생산량 저하에 대응할 수 있는 의사지원시스템
이 필요하다. 이에 본 연구는 밀을 대상으로 재배관리 의사결
정 지원시스템을 웹 기반으로 설계하고 구축하였다. 이 시스
템은 생육단계에서의 기상환경을 1일 단위로 수집 및 측정하
여 토양수분 함량을 예측할 수 있는 기능을 설계하였다. 이를 
기반으로 과정기반 작물모형 중 하나인 APSIM을 이용하여 
밀 재배 단계별 의
```
---

# 9. 추가작업

- step5.py, step6.py를 OpenAI text-embedding-3-large model를 사용한 예제 변경
- step5.py, step6.py를 Gemini models/gemini-embedding-001를 사용한 예제 변경
